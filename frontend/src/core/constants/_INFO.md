# Constants Subsection

Constants cannot change. This subsection centralizes all the immutable values for easier app updates for example changing a default port for all servers in one place.

## Files

1. 'statusConfig.ts' : Will map status with icon, colors

2. 'default.ts' : Defines the global default values for example RAM limits, backup frequencies, etc.